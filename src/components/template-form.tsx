"use client";

import { useRouter } from "next/navigation";
import { useState } from "react";
import { createTemplate, updateTemplate } from "@/app/actions/templates";

const weekdays = ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sáb"];
type TemplateFormValue = { id?: string; title: string; category: string; defaultStartTime: string; defaultDurationMinutes: number; daysOfWeek: number[] };

export function TemplateForm({ template, onCancel, onSaved }: { template?: TemplateFormValue; onCancel?: () => void; onSaved?: () => void }) {
  const router = useRouter(); const [error, setError] = useState(""); const [pending, setPending] = useState(false);
  async function submit(formData: FormData) { setPending(true); setError(""); const input = { title: String(formData.get("title")), category: String(formData.get("category")), defaultStartTime: String(formData.get("defaultStartTime")), defaultDurationMinutes: Number(formData.get("defaultDurationMinutes")), daysOfWeek: formData.getAll("daysOfWeek").map(Number) }; try { if (template?.id) await updateTemplate(template.id, input); else await createTemplate(input); router.refresh(); onSaved?.(); } catch (e) { setError(e instanceof Error ? e.message : "Não foi possível salvar a rotina."); } finally { setPending(false); } }
  return <form action={submit} className="panel routine-form"><div><h2>{template ? "Editar rotina" : "Nova rotina"}</h2><p className="help-text mt-1">{template ? "Atualize todos os campos da rotina." : "Cadastre uma vez e deixe o dia nascer organizado."}</p></div><label className="field-label">Nome<input className="input" name="title" placeholder="Faculdade" defaultValue={template?.title} required /></label><label className="field-label">Categoria<input className="input" name="category" placeholder="Estudo" defaultValue={template?.category} required /></label><div className="grid grid-cols-2 gap-3"><label className="field-label">Horário<input className="input" name="defaultStartTime" type="time" defaultValue={template?.defaultStartTime.slice(0, 5)} required /></label><label className="field-label">Duração (min)<input className="input" name="defaultDurationMinutes" type="number" min="1" step="1" defaultValue={template?.defaultDurationMinutes ?? 60} required /></label></div><fieldset><legend className="mb-2 text-sm font-bold">Dias da semana</legend><div className="routine-days">{weekdays.map((day, index) => <label key={day} className="day-check"><input name="daysOfWeek" type="checkbox" value={index} defaultChecked={template?.daysOfWeek.includes(index)} />{day}</label>)}</div></fieldset>{error && <p className="alert alert-error">{error}</p>}<div className="flex gap-2"><button className="btn btn-primary" disabled={pending}>{pending ? "Salvando..." : template ? "Salvar alterações" : "Criar rotina"}</button>{onCancel && <button type="button" className="btn btn-quiet" onClick={onCancel}>Cancelar</button>}</div></form>;
}
