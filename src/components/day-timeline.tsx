import { TaskControls } from "./task-controls";
import type { DailyTask } from "@/lib/domain/types";
const statusLabel = { planned: "Planejada", running: "Em andamento", paused: "Pausada", completed: "Concluída" };
export function DayTimeline({ tasks, activeTaskId }: { tasks: DailyTask[]; activeTaskId?: string }) { if (!tasks.length) return <div className="panel empty-state"><h2>Seu dia está aberto</h2><p>Crie uma rotina ou adicione uma tarefa avulsa para começar.</p></div>; return <div className="timeline"><div className="timeline-line" />{tasks.map((task) => <article className="timeline-item" key={task.id}><time className="timeline-time">{task.plannedStart.slice(0, 5)}</time><div className={`panel task-card ${task.status === "running" ? "running" : ""}`}><div className="task-card-top"><div><h2 className="task-title">{task.title}</h2><p className="task-meta">{task.category} · {task.plannedDurationMinutes} min planejados</p></div><span className={`status ${task.status === "completed" ? "status-done" : "status-muted"}`}>{statusLabel[task.status]}</span></div><TaskControls task={task} activeTaskId={activeTaskId} /></div></article>)}</div>; }
