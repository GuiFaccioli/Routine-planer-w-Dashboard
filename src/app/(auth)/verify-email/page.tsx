import Link from "next/link";
import { VerifyEmailForm } from "@/components/verify-email-form";

export default async function VerifyEmailPage({ searchParams }: { searchParams: Promise<{ email?: string }> }) { const { email = "" } = await searchParams; return <main className="auth-canvas"><aside className="auth-aside"><span className="brand">ritmo<span className="brand-mark">.</span></span><div><h1>Uma última confirmação.</h1><p>Verifique seu email para manter sua conta e seus planos seguros.</p></div></aside><section className="auth-main"><div className="auth-card"><div className="auth-card-header"><p className="eyebrow">Confirmação</p><h1>Confirme seu email</h1><p className="help-text mt-2">Digite o código de confirmação enviado para seu email. Se não chegou, você pode reenviá-lo.</p></div><VerifyEmailForm initialEmail={email} /><Link className="text-link mt-4 inline-block" href="/sign-in">Voltar para entrar</Link></div></section></main>; }
